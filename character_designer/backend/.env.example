# Backend Configuration
PORT=5174
NODE_ENV=development

# Database
DATABASE_PATH=./data/characters.db

# AI Configuration - OpenRouter
# Get your FREE API key at: https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-your-api-key-here

# Model Selection (change to switch AI models)
#
# RECOMMENDED (Best balance of cost & quality):
#   openai/gpt-3.5-turbo                     - $0.0015 per character, reliable (DEFAULT)
#   anthropic/claude-3.5-haiku               - $0.001 per character, fast
#
# FREE MODELS (May not always generate perfect 32x32 grids):
#   meta-llama/llama-3.1-8b-instruct         - FREE, sometimes struggles with precision
#   meta-llama/llama-3.1-70b-instruct        - FREE, better quality than 8B
#
# PREMIUM MODELS (Best quality, most reliable):
#   anthropic/claude-sonnet-4.5              - ~$0.003 per character (excellent)
#   openai/gpt-4o                            - ~$0.0025 per character (fast & reliable)
#
# NOTE: Free models may not always follow the exact 32x32 grid requirement.
#       For best results, use GPT-3.5-turbo or premium models.
#
OPENROUTER_MODEL=openai/gpt-3.5-turbo

# CORS (frontend URL)
FRONTEND_URL=http://localhost:5173
